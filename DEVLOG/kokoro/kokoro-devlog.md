- 1 | 2026-04-18 21:18:51: brutal v1-normalized verdict: 'Maya O'Brien' mangled → 'mayaobree's dee defense' (no Ph.D. rendering). Rest slightly better but not great. Kokoro needs its own customized normalization — different phonemizer stack from Kitten. User asked for web research on github kokoro-tts normalization improvements.
- 2 | 2026-04-18 21:22:56: Research on kokoro normalization. Key findings: (1) FluidAudio uses pure CoreML BART G2P — NOT espeak/misaki — so our Kitten-tuned normalizer has limited overlap. (2) FluidAudio natively supports SSML in synthesize(): <sub alias>, <phoneme alphabet=ipa ph=>, <say-as interpret-as=characters|cardinal|ordinal|digits|date|time|telephone|fraction>, plus markdown [word](/ipa/). This is the proper seam for Dr./Ph.D./a.m./O'Brien/acronyms.
- 3 | 2026-04-18 21:22:56: Research cont'd. (3) Misaki's own en.py does NOT do Mr./Dr./Ph.D. expansion in code — it relies on gold/silver lexicon + espeak fallback + NFKC+quote normalization in preprocess(). (4) remsky/Kokoro-FastAPI normalizer.py has mature regex for EMAIL/URL/UNIT/MONEY/NUMBER/TIME/phone + VALID_UNITS dict — patterns worth porting. (5) mlalma/MisakiSwift exists as SPM package — possible drop-in G2P swap if FluidAudio CoreML G2P proves worse than misaki's.
- 4 | 2026-04-18 21:30:58: SSML normalizer wired (task #55). Fixed Ph.D. double-wrap. Preview: 'Doctor Maya O'Brien's P H D defense starts at nine thirty A M; F Y I please R S V P... twenty twenty-six April eighteenth... one thousand two hundred thirty-four dollars and fifty-six cents...'.
- 5 | 2026-04-18 21:30:58: SSML v3 render: outputs/brutal/kokoro-ssml-v3/kokoro-fluidaudio.wav. Remaining issues for iteration: QF12 letter-split leaks 12 as attached number, 3.14 decimal not normalized, IPv4 dots read as pauses.
- 6 | 2026-04-18 21:40:03: Root cause of v3 'too terrible': FluidAudio SSMLProcessor.countWordsBeforeIndex counts literal tag-character words (sub, alias, say, as, interpret) in the prefix before each tag, so <phoneme> overrides land at garbage word indices and corrupt synthesis.
- 7 | 2026-04-18 21:40:03: Fix: drop all <phoneme> tags; names go via <sub alias="Oh Bryan">O'Brien</sub> with plain-English phonetic spellings. <sub> and <say-as> paths don't touch phoneticOverrides, so safe. Render: outputs/brutal/kokoro-ssml-v4/kokoro-fluidaudio.wav.
- 8 | 2026-04-18 21:50:54: SSML v6 fixes: read→red broadened (she/he/they/we/I/you/it/who/which/that + nouns), IPv4 →'I P version four', 192.168.1.1 →digit-dot alias, 3.14% →'three point one four percent', QF12 →alias 'Q F 1 2', JSON added to wordAcronyms. Fixed nested-tag regression via digit/< / > lookbehind in initialism regex. Output: outputs/brutal/kokoro-ssml-v6/kokoro-fluidaudio.wav.
- 9 | 2026-04-20 14:03:28: Phase 5 review follow-ups. P1: dropped tokenize-preview's --normalize-punct flag — it validated round-trip against the normalized string, a silently-different contract. Preview now always verifies raw-in → tokens → source-slice concat == raw minus whitespace. P2: narrowed KokoroPunctuation drop-to-space set — ASCII glyphs with downstream semantics (/ @ % $ & * + = _ # °) now survive so Phase 7 normalize→tokenize won't erase URL/email/percent/temp forms. Round-trip 145/145 unchanged.
- 10 | 2026-04-20 14:17:53: Phase 7 landed. KokoroG2P.resolve → G2PResult(tokens, overrides, structuralSpans) as pure additive analysis. Pipeline: normalize → tokenize+POS → proper-name chain (full-name/compose/respell/sub-alias) → Penn-context overlay → homograph w/ copula-veto + hand-tuned overlay → OOV no-op. Plain→token.phonemes (custom-lex); homograph/name/Penn→overrides (markdown); sub-alias→structuralSpans. g2p-preview CLI added. normalize-preview unchanged on 9 fixtures.
- 11 | 2026-04-20 14:23:42: Phase 7 review discovery: KokoroG2P drops safe DEFAULT pronunciations for words whose dict entry is POS-keyed but only with non-universal alternates (e.g. there DEFAULT+None, that DEFAULT+DT, used DEFAULT+VBD). Per-token walk only tries lookupPlainTokens, so these fall through to OOV instead of using DEFAULT. g2p-preview confirms there/that/used unresolved while read/read-past still resolve. Matrix/Smooth still synthesize raw text via KokoroTtsManager and do not exercise the ported path.
- 12 | 2026-04-20 14:33:02: Phase 7 review P1: per-token fall-through used lookupPlainTokens (returns nil for any POS-keyed entry), silently dropping safe DEFAULT for there/that/non-habitual used — violates plan rule G. Switched to KokoroLexicon.lookup(word) — DEFAULT for POS-keyed, plain for plain. Guarded homographRules members so null-DEFAULT rules don't re-resolve. Net: OOV drops on all 9 fixtures (gulliver −18, quiet-house −5, foot-massage −7); normalize-preview hashes byte-identical.
- 13 | 2026-04-20 14:52:41: Phase 8 landed. KokoroG2P.emit splices overrides + structural spans into normalizedText; lexiconEntries feeds TtsCustomLexicon. KokoroSSMLNormalizer.compensatorsOnly = structural-only path (skips Celtic/Penn/gold/homograph). Runner branches on KOKORO_G2P=ported (default classic). KokoroMatrix/Smooth documented as raw baselines per checkpoint H. normalize-preview --ported added.
- 14 | 2026-04-20 14:52:56: Phase 8 diff vs classic: foot-massage/homographs/trouble byte-identical. Remaining fixtures differ in 2 expected classes. (1) Ex-gold-override words (Maya/colonel/kettle/boil/February/…) now route via TtsCustomLexicon plain-lookup, not markdown. (2) micro-corpus 'live' after A.I.: classic misses the wrap (NLTagger on SSML'd text); ported runs on clean text pre-emission and wraps both occurrences. Ported is the correct path.
- 15 | 2026-04-20 17:57:07: Phase 8 review: ported path still over-trusts NLTagger on some noun/verb homographs. Natural probes misread plural-subject finite verbs like 'Muscles contract...', 'Models project...', and 'Trainees progress...' as noun/default, while 'Researchers conduct...' and 'Consultants present...' resolve correctly. Likely fix is extra cue-based overlay or broader fixture-backed fallback in KokoroG2P.resolveHomograph.
- 16 | 2026-04-20 18:08:22: Phase 8 P1 fix: plural-subject verb promotion. NLTagger under-tagged finite verbs after bare plural subjects ('Muscles contract', 'Models project', 'Trainees progress'). resolveHomograph now promotes Noun→Verb when rule has a Verb variant, a prior plural-noun subject exists, and no prior Verb token. Guards ('The contract' / 'Her contract') stay on DEFAULT. Fixture gains 6 correct VERB resolutions (bow, contract×2, subject, project×2, progress, permit).
- 17 | 2026-04-20 19:21:31: Phase 9a hermetic tests landed. 16 @Suite tests, 60+ parameterized cases in Tests/TTSHarnessCoreTests. NormalizerOutputTests (22 SSML goldens), TokenSpanTests (round-trip across 9 fixtures), LexiconProvenanceTests (8 cases), POSDecisionTests (22 probes + 4 noun-guard regressions), FootMassageGoldenTrace (full-passage golden). New Passage.loadFixture helper exposes bundled resources to the test target.
- 18 | 2026-04-20 19:21:37: Phase 9a PostFluidAudioTests landed as FluidAudioRunnerTests target, gated behind INTEGRATION=1 because it downloads the Kokoro model. Asserts synthesizeDetailed returns ≥1 chunk and the state-of-the-art hyphen compound survives FluidAudio's second preprocessing pass — canary for span-eating regressions. Finer chunk assertions wait on upstream debug hook (checkpoint J.1).
- 19 | 2026-04-20 19:26:43: Phase 8 follow-up review: plural-subject promotion is both over- and under-inclusive. It wrongly promotes noun compounds after plural nouns ('The analysts project manager', 'The trainees progress report') and still misses irregular-plural subjects ('Children present', 'Women record', 'People project'). Phase 9a tests cover s-plural wins but not these guard/failure cases.
