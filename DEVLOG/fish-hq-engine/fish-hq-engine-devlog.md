- 1 | 2026-08-01 16:04:58: Shipped 'mako say --hq': fish-s2-pro-8bit behind Kokoro, opt-in per call, weights never resident. Sidecar at Sources/TTSHarnessCore/Resources/fish_say.py is deliberately self-contained (~100 lines duplicated from gen_fish.py/oomguard.py) -- inside the resource bundle it has no siblings to import, and that target already declares .process(Resources) so no manifest edit was needed. Pinned mlx-audio==0.4.6 + uv lock --script.
- 2 | 2026-08-01 16:04:58: Verified --hq end to end: 1570 chars -> 97.9s audio in 195s wall (RTF ~0.5), F0 held 128-162 Hz, clone signature 153.9 Hz matching gemini2-22. Short render peaks 9.7 G; model load 4s warm.
- 3 | 2026-08-01 16:04:58: Process-group spawning was the load-bearing detail. Foundation Process has no pre-exec hook on Darwin and parent-side setpgid races uv forking python, so Sidecar.swift uses posix_spawn + POSIX_SPAWN_SETPGROUP. That removes the child from the terminal's foreground group, so Ctrl+C stops reaching it -- without forwarding, interrupting leaves a 6.8 GB orphan. SIGINT/SIGTERM handlers kill(-pgid, SIGKILL). Verified: 3 procs killed, mako exits 130, pgrep empty.
- 4 | 2026-08-01 16:04:58: vm_statistics64.free_count INCLUDES speculative_count (Apple's vm_stat subtracts it before printing 'Pages free'), so MemoryWatchdog uses available = free - speculative + inactive + purgeable. The naive formula over-counts by exactly the pool we mean to exclude. Watchdog lives in the parent because a thrashing child may not get scheduled to run its own guard. Forced trigger via MAKO_OOM_FLOOR_MB=30000: exit 137, readable reason, no orphan.
- 5 | 2026-08-01 16:05:07: Made --hq normalization OFF by default, deviating from the plan. Evidence: fish is a Qwen3-omni LLM that reads $1,234.56 and 7:03 a.m. natively, and every render judged by ear during the bakeoff -- including the number-heavy itin torture passage -- went in as raw text. Normalization is available as --normalize (compensatorsOnly + flattenAliases) for A/B, but it leaves digit-bearing aliases like '7 03 A M' that only make sense to FluidAudio's second pass. Needs an ear to settle.
- 6 | 2026-08-01 16:05:14: group_turns_into_batches refuses to split an oversized turn, and max_tokens then truncates that batch -- so a paragraph over chunk_length silently loses its tail. FishText.segment subdivides at sentence boundaries via NLTokenizer (already knows Dr., a.m., 2.7).
- 7 | 2026-08-01 16:05:14: KokoroNumbers/KokoroAbbreviations emit SSML <sub alias> spans, which fish would read aloud -- FishText.flattenAliases collapses them. They also mis-read 7:03 a.m. as 'seven to three' unless the whole compensatorsOnly order runs first (wrapTimeMeridiem has to claim it before the ratio handler).
- 8 | 2026-08-01 16:48:19: Reviewer fixes. (1) FishSetup's --check now runs 'uv run --offline': without it a machine that never ran 'mako hq install' would have uv silently build the whole script env just to report weights missing, with stderr discarded. Verified: empty HF_HUB_CACHE -> doctor fails in 0.38s, 0B downloaded. (2) MemoryWatchdog gained a stopped flag so a late start (child died before the detached start task ran) cannot poll on a reusable pgid. (3) Sigh regex word-bounded.
- 9 | 2026-08-01 16:48:19: The sigh regex had a second bug the review missed: the paren branch was \(sighing?\), where ? binds to g -- so it matched (sighin) and (sighing) but NOT (sigh). Now \(sigh(?:s|ing|ed)?\). Verified both directions: [sigh] [sighing] [SIGH] [deep sigh] [sighs] (sigh) (sighing) all vanish, [sad][sighing] keeps [sad], and [insightful tone] [sight unseen] [foresight] all survive.
- 10 | 2026-08-01 16:48:19: Long-passage verification, the 14.5 GB regime the watchdog exists for: itin-long.txt 6644 chars -> 367.5s audio, 18m39s wall, exit 0, watchdog silent. phys_footprint_peak 14 GB measured with 'footprint -p' (RSS reports only ~65 MB because MLX allocates through Metal, so ps is useless here). Free pages fell to 65 MB with 3.9 GB inactive; available stayed above the 2000 MB floor throughout.
- 11 | 2026-08-01 16:48:19: Long renders were silent for 18 minutes because FishRunner captured the sidecar's stderr and only surfaced it on error. Sidecar.readToEnd now takes an optional sink that forwards chunks as they arrive, wired to FishOptions.progress (on unless --quiet). Also: markers verified end to end -- parakeet transcribes '[laughing] Sorry about that. [confident] The fix is correct...' back with the tags absent.
- 12 | 2026-08-01 17:19:56: Release v0.4.0 failed to build in Homebrew and exposed a pre-existing packaging bug: Package.resolved was gitignored, so the tarball shipped no pins and 'from:' caret ranges let a clean build resolve FluidAudio 0.15.5 (which removed TtsCustomLexicon) and speech-swift 0.0.23 instead of the tested 0.13.6/0.0.9. v0.3.0 was equally broken for anyone installing fresh. Fixed by tracking Package.resolved; verified with a git-archive clean-room release build before cutting v0.4.1.
