- 1 | 2026-07-30 18:32:13: Bakeoff harness scripts/tts-gen/ renders the 5 passages through fish-audio-s2-pro (bf16+8bit), Qwen3-TTS-12Hz-1.7B-8bit, VoxCPM2, Inflect-Micro-v2. Two loader fixes: PowerBeef02 Qwen3 quantizes talker.model.text_embedding, which mlx-audio's model_quant_predicate skips - it built a plain Embedding then rejected .scales/.biases; narrowed the skip list (stock repos still fine, apply_quantization falls back to checking .scales in weights).
- 2 | 2026-07-30 18:32:13: mlx-audio's voxcpm2 family cannot load the official openbmb/VoxCPM2 checkpoint (rejects audio_vae.encoder.*) - it targets an MLX conversion. VoxCPM2 therefore runs via the official voxcpm package on MPS, which auto-downgrades bf16->fp32. Speeds on M5 as realtime multiples: Inflect 0.3x, Qwen3 1.1x, fish-8bit 2.3x single-call (4.1x chunked), VoxCPM2 4.9x.
- 3 | 2026-07-30 18:32:34: Voice lurch mid-passage was my harness, not the models: cloning models sample a fresh speaker per generate() call, so one call per chunk = one speaker per chunk. Verified: F0 fell 170Hz -> 105Hz exactly at the chunk-2 boundary; single-call holds 170-203Hz (and is 2x faster). Fix: --mode single. VoxCPM2 can't (max_tokens 2000 ~ 320s) so it pins chunk 1 via prompt_wav_path/prompt_text. check_voice.py reports per-window F0/centroid to catch this.
- 4 | 2026-07-30 18:32:34: Cost real compute: I hand-rolled a [.!?] splitter for the bakeoff and it corrupted the exact text micro-corpus tests - 2.7 -> '2. 7', A.I. -> 'A. I.', 9:30 a.m. -> '9:30 a. m.'. Repo already solved it twice: SupertonicTokenizer.chunk only splits when whitespace follows the punctuation, and KokoroAbbreviations.swift has 176 dotted surfaces. chunking.py now reads that table at runtime and self-tests a verbatim round-trip. Qwen3TTSCoreMLRunner.chunk still has the naive bug.
- 5 | 2026-07-30 19:38:36: OOMed the machine by running two heavy gens at once (fish-bf16 ~10GB chain + VoxCPM2 fp32 ~8GB on MPS). macOS doesn't fail fast - it thrashed for minutes. Added scripts/oom_watchdog.sh (external, adapted from llama-testing: RAM floor + swap-storm + log signature -> SIGKILL, 0.3s poll, exit 42) and scripts/tts-gen/oomguard.py (in-process arm() before model load, --oom-floor-mb). Wired into all 3 gen scripts.
- 6 | 2026-07-30 19:38:36: New CLAUDE.md <generation-rules>: watchdog mandatory on every generation run, cancel-and-restart anything running without one, kill brutally (SIGKILL) near OOM rather than waiting for an allocation to fail, don't raise the floor to make a big model fit, and SEQUENTIAL ONLY - never two gens at once. Verified the guard fires: fake floor -> self-SIGKILL exit 137; and it really killed fish-8bit at 2526MiB/2465MiB instead of hanging the Mac.
- 7 | 2026-07-30 19:38:36: Machine state matters for these gens: after the OOM, swap sat at 10GB/11.2GB used and grew to 14GB during fish-8bit. MLX transiently ~doubles RAM while loading safetensors, so fish-8bit (6.3GB on disk) spiked available RAM from ~11GB down under 2.5GB. fish-bf16 is 10.25GB and likely not runnable in this state.
- 8 | 2026-07-30 19:38:46: single-call mode doesn't scale to the 8k homographs passage: qwen3 ran 17min with no output (AR decode with a growing KV cache) before I killed it. Chunked + pinned voice is now the uniform mode for every AR model - bounded cost per call AND one speaker. qwen3 all 5 passages landed clean that way (homographs 576s audio in 427s wall).
- 9 | 2026-07-30 19:38:54: fish-8bit chunked+pin produced mostly-failed passages (homographs 16.4s not ~600s; chunks 21-33 all errored inside the same second = broken model/Metal state, not per-chunk content). diag_chunk_failures.py ruled content out: plain ascii, IPA in parens, '===' banners, embedded newlines and the verbatim failing chunk ALL generate fine without ref_audio. Suspects left: the ref_audio pinning path, or Metal erroring under swap pressure.
- 10 | 2026-07-30 19:38:54: Debugging hygiene: gen_mlx.py deliberately swallows a failing chunk (one bad chunk shouldn't cost a passage) and my 'rg ... | tail -14' pipeline then threw the tracebacks away, so a whole run's failure reason was unrecoverable. Log generation runs to a file and grep that; never pipe the only copy through tail.
- 11 | 2026-07-30 19:46:30: Root cause of the fish chunk failures: mlx-audio's fish_qwen3_omni wants ref_audio as a decoded mx.array, not a path - _prepare_reference_prompt does 'audio.ndim' immediately, so a str raises AttributeError before any generation. Every pinned chunk failed instantly, leaving only chunk 1 per passage (which is exactly why homographs was 16.4s). qwen3_tts tolerated a path, which masked it.
- 12 | 2026-07-30 19:46:30: Fix: gen_mlx.reference_argument() mirrors generate_audio()'s own rule - pass the path when the model sets preserve_ref_audio_path, else load_audio(path, sample_rate=model.sample_rate). Calling model.generate() directly bypasses that wrapper, which is how I lost it. Verified on fish-8bit: raw path FAILED, reference_argument() OK. diag_chunk_failures.py now probes both forms so this can't regress silently.
- 13 | 2026-07-30 20:04:12: The OOM kills during fish-8bit were self-inflicted via --max-tokens: I raised the default to 32768 for single-call mode, and these AR models size their KV cache from max_tokens, so the guard SIGKILLed the run mid-passage (exit 137) even with 17-19GB available. Back to 4096, which is generous for a <=300-char chunk (fish ~21Hz, qwen3 ~12Hz = a few hundred tokens). brutal then completed clean with mlx active flat at 6.8G.
- 14 | 2026-07-30 20:04:12: Added mx.clear_cache() plus a per-chunk memory note (mlx active/cache + available RAM) to gen_mlx.py, and memory logging before/after model load. That trace is what localised the max_tokens blowup: active stayed 6.8G while available swung 6-15G per chunk, so the spike was per-call KV allocation and not a leak.
- 15 | 2026-07-30 21:28:55: Fish monotony cause 1: we chunked a 434-char passage for nothing. _split_generation_text returns the text UNCHANGED unless it contains <|speaker:N|> tags, so chunk_length is inert for plain prose and fish never wanted external chunking here.
- 16 | 2026-07-30 21:28:55: Fish monotony cause 2: fish's long-form loop appends each batch's VQ codes back into the running Conversation as an assistant turn, so its internal batches carry prosodic context. One generate() per chunk discards that — every chunk is a cold start with a fresh system prompt.
- 17 | 2026-07-30 21:28:55: Fish monotony cause 3: --pin-voice fed chunk 1's own audio back as ref_audio. With a reference present the system prompt becomes 'convert ... reference to the following', so the reference dictates delivery and not just timbre — a prosody echo chamber locked to a flat opening sentence.
- 18 | 2026-07-30 21:28:55: Unused fish S2 Pro prosody levers: instruct= (becomes 'Style instruction: ...' in the system prompt via _build_conversation) and inline [tag] markup for word-level control ([emphasis] [pause] [whisper] [chuckle], ~34 tags). Sampling defaults are conservative: temp 0.7 / top_p 0.7 / top_k 30.
- 19 | 2026-07-30 21:28:55: Levers objectively bite: same micro-corpus text renders 38.5s at baseline vs 47.4s with instruct+warm sampling, 23% longer — varied pacing, not just a different sampled voice. Variants in scripts/tts-gen/exp_fish_prosody.py -> wav-fish-prosody/.
- 20 | 2026-07-30 21:29:08: Qwen3-TTS voices: the checkpoint we benchmarked (PowerBeef02/...-Base-8bit) has NO built-in voices — config says tts_model_type 'base' with spk_id {}, so get_supported_speakers() is empty and every voice heard was randomly sampled per call. Named voices live in the CustomVoice family (9: serena vivian uncle_fu ryan aiden ono_anna sohee eric dylan), which also takes instruct= for emotion. VoiceDesign builds a voice from a text description.
- 21 | 2026-07-30 21:29:08: MLX memory: my memory_note() called mx.clear_cache() before reading, which HID the growth that was getting runs SIGKILLed — active looked flat at 6.8G while system available RAM drained. Fix: report active/cache/peak/avail without clearing, and mx.set_cache_limit() so freed buffers go back to the OS. get_peak_memory() then exposed real transient spikes of 15-17G.
- 22 | 2026-07-30 21:29:08: Fish single-call on a 434-char passage does not fit: 6.8G of weights plus a one-pass 44.1kHz decode of ~40s audio drove available RAM to 2.19G and the watchdog killed it, even at max_tokens 1536. Fix that solves memory AND monotony together: tag each sentence <|speaker:0|> so fish batches internally with its running Conversation. Peak fell 16G+ -> 12.4G, active flat 6.8G, and prosody now carries across batches.
- 23 | 2026-07-30 21:29:08: exp_fish_prosody.speaker_tagged() splits via chunking.split_sentences so 2.7 / A.I. / 9:30 a.m. survive; asserted verbatim round-trip before spending GPU time. Also: max_tokens sizes the KV cache, so oversized values (4096 for ~850 needed tokens) are a direct OOM cause — set it from expected audio length x codec rate.
- 24 | 2026-07-30 21:37:59: Verified the 9 CustomVoice voices are genuinely distinct via check_voice.py --summary: median F0 spans 122Hz (aiden) to 233Hz (serena). VoxCPM2 seeded speakers are far less diverse — seeds 2/3/5 all land at 193-197Hz, so 5 seeds yield only ~3 distinct voice types. Named voices beat seed roulette for variety.
- 25 | 2026-07-30 21:37:59: Added check_voice.py --summary: per-file median F0 (identity), mean within-window p10-p90 F0 span in semitones (flatness proxy), drift (std of window medians = speaker movement). Among fish variants sharing one sampled speaker, span rises 11.15 -> 18.39 st from stock to instruct+warm. Caveat: cross-file span comparisons are confounded by speaker pitch and by any speaker switch, so the old chunked baseline's 18.09 is NOT evidence it was expressive — it had a mid-file voice lurch.
- 26 | 2026-07-30 23:03:53: CONFIRMED by ASR: fish reads instruct= aloud when there is no reference audio. 04-instruct-lively.wav transcribes as 'Style instruction. Read this as an engaging audiobook narrator...'. Cause is _build_conversation's no-reference branch: the system turn just ends with 'Style instruction: ...', nothing marks where speech begins.
- 27 | 2026-07-30 23:03:53: But instruct= DOES work when paired with reference audio: villain-clone-instruct.wav opens with 'Morning Robert. Here's the list.' — no leak. The reference branch wraps things in 'Text:\n' ... '\n\nSpeech:\n', which contains the instruction. So style control on fish requires --clone-wav; instruct alone is unusable.
- 28 | 2026-07-30 23:03:53: Voice reproduction on fish: seeds do NOT reproduce a voice across different text (same seed 11, itin instead of micro-corpus, gave F0 143.5 vs 107.5 — a different speaker). Cloning does: villain-clone 106.4Hz and villain-clone-instruct 107.1Hz vs source 107.5Hz. Cloning from an expressive clip also raised pitch span 15.96 -> 21.3 st and cut drift 4.3 -> 2.8, so it did not flatten delivery.
- 29 | 2026-07-30 23:03:53: Chunking qwen CustomVoice cuts peak memory hard, not just enabling long text: brutal in one 896-char pass peaked 15.8-19.8G, while itin at 1570 chars in 4 chunks of 500 peaked only 9.2G. Chunking is safe for identity here because the speaker is a fixed named voice and cannot drift — unlike the reference-free cloning models. _generate_with_instruct does no internal splitting.
- 30 | 2026-07-30 23:03:53: transcribe_check.py trap: parakeet returns EMPTY for some windows of good audio. On one 98s render, 0-8s and 40-56s were correct but 0-30s, 8-24s and the full file were all empty. I briefly concluded the renders were broken; they were fine. Probe ~8s windows and check waveform RMS before believing an empty transcript.
- 31 | 2026-07-31 12:49:03: WRONG earlier claim corrected: a fixed named CustomVoice speaker does NOT prevent voice drift across chunks. wav-qwen-itin/vivian.wav steps +64Hz at ~40-44s and -52Hz at 116-120s, both at chunk boundaries (chunks ended ~40.7s/79.7s/119.2s), plus a general climb to ~250Hz mid-file. The speaker embedding fixes timbre; each generate() call still re-samples register and energy. Listener called it 'extremely jarring'.
- 32 | 2026-07-31 12:49:03: mlx-audio bug: MODEL_REMAPPING is dead whenever model_name is passed. get_model_class does model_type_mapped = remapping.get(model_type), then guards the branch that uses it behind elif, while the if-branch fires on 'model_name is not None and model_type_mapped != model_type' — true exactly when a remap is needed. It then scans repo-name parts, matches nothing, and imports the unmapped type. bosonai/higgs-tts-3-4b failed as 'higgs_multimodal_qwen3 not supported' despite the family shipping.
- 33 | 2026-07-31 12:49:03: Workaround in gen_mlx.patch_model_remapping(): pass model_name=None when model_type has an exact remapping, forcing the working branch. Also needs torch at runtime — higgs_audio codec loads shards with safe_open(framework='pt') — supplied via 'uv run --with torch'. bosonai/higgs-tts-3-4b then rendered itin: 87.5s audio in 172.5s wall, active 8.2G, peak 11.2G.
- 34 | 2026-07-31 12:49:03: VibeVoice blocked on this Mac, both requested repos. DevParker/VibeVoice7b-low-vram is bitsandbytes nf4 (CUDA-only, no MPS support). cstr/vibevoice-1.5b-GGUF targets the CrispASR runtime, not stock llama.cpp or mlx-audio. mlx-audio does ship a 'vibevoice' family, and its base model microsoft/VibeVoice-1.5B is torch safetensors — untried, would be a substitution.
- 35 | 2026-07-31 13:00:13: microsoft/VibeVoice-1.5B will not load in mlx-audio's vibevoice family: 'Missing 247 parameters', e.g. tts_language_model.layers.9.self_attn.v_proj.weight. The family expects tts_language_model.* keys the microsoft checkpoint does not ship, and its only MODEL_REMAPPING entry is vibevoice_streaming -> vibevoice, so it appears written for the streaming/Realtime conversions (mlx-community/VibeVoice-Realtime-0.5B-*), not the original 1.5B.
- 36 | 2026-07-31 13:00:13: Fish chunk-length 400 vs 150 on itin: 6 internal batches instead of ~11, 102.5s audio in 244.3s wall, but peak memory rose 11.6G -> 15.3G because each batch decodes more audio at once. Survived at floor 2500. Note fish's own API docs put chunk_length range at 100-300 (default 200), so 400 is above spec; 300 is the sanctioned ceiling.
- 37 | 2026-07-31 13:00:13: Research: S2 inline tags are NOT the ~34 in the bundled README — the model takes free-form natural-language descriptions, 15000+ tags, e.g. [speaking slowly, almost hesitant] or [dead tired, end of a very long shift]. Best practice: place immediately before the affected words, always follow a descriptive tag with text (orphaned tags are unpredictable), combine physical+emotional ([panting] [tired]), and do not over-tag. This is the strongest unused lever for the flatness.
- 38 | 2026-07-31 13:00:13: Research: fish's own defaults are temp 0.7 / top_p 0.7 / rep 1.2, with 0.7-0.8 recommended for expressive natural speech — our 'hot' preset (1.0/0.95/80) is above spec, which fits it sounding fun but unstable. Reference clip delivery IS the style setting (10-30s clip carries timbre, speaking style and emotional tendency), so cloning our own flat output was self-defeating. Chunk-to-chunk voice inconsistency is a known upstream bug (fish-speech issue 836).
- 39 | 2026-08-01 00:39:40: Which 30s you clone from IS the voice, and one sample is a lottery — sweep_clone_windows.py rolls the reference window across a recording in 5s steps (20 windows) and renders a short numbered clip from each ('Number N' spoken, so they sort by ear). Stages split into separate processes: parakeet for all transcripts, then fish for all renders. Edges snap to the quietest 100ms within +/-1.2s so no reference starts mid-word; offsets in clone-windows/manifest.json.
- 40 | 2026-08-01 00:39:40: Fish OOM lever is max_tokens, not chunk_length: it sizes the KV cache, and the 1536 default is ~3x what a 400-byte batch needs. First sweep run was SIGKILLed on render 1 (only 11.4G avail; other apps had grown); max-tokens 900 + mlx-cache 512MB held peak at 13.3G with avail never under 8.3G across all 20 renders, ~120s wall per 58s clip.
- 41 | 2026-08-01 00:39:40: Clone-window sweep result: all 20 windows land 162-179 Hz against the 188.4 Hz reference, span 21-24 st. So fish clones consistently ~15 Hz below its reference, and window choice only moves pitch ~17 Hz — far less than clone-vs-seed (seeded hot-cl400 was 101 Hz). Pick a window by ear/accent, not by expecting pitch to shift much.
- 42 | 2026-08-01 11:58:57: Fish length scaling measured (7119 chars = 4x itin, 24 batches, 375.6s audio, 864s wall, RTF 0.43): peak RAM stayed FLAT at 14.4G, active grew only 6.8->6.9G. Corrects my earlier claim that the growing conversation makes memory scale with length — it does not. The cost is time: ~28s/batch early vs 37-43s late from re-prefilling history. Voice held over 6 min: F0 148.3 vs 151.2 at 1x, drift 10.6 vs 11.7, span 18.6 vs 20.7.
- 43 | 2026-08-01 11:58:57: Fish cloning has no per-voice model to save: _prepare_reference_prompt runs codec.encode(ref_audio) and prepends the VQ codes as the first assistant turn. Production ships the 29.3s wav + its exact transcript as runtime resources. Note semantic_token_budget = min(max_tokens, text_token_count*12), so max_tokens is only a ceiling — for a 400-byte batch the inner term (~1200) usually binds.
- 44 | 2026-08-01 13:09:17: Anchored-history experiment REFUTED the dilution hypothesis. gen_fish_anchored.py rewrites history per batch (free — fish rebuilds the KV cache each batch anyway). Unbounded vs window-3+pin-first vs +reference re-injection, on 6645 chars of distinct prose: span still fell ~5 st in all three (-5.26 / -6.39 / -5.70), and the user judged the anchored renders worse by ear. Only gain was ~6.5% speed. Keep fish's unbounded history.
- 45 | 2026-08-01 13:09:17: S2 Pro has documented Fine-Grained Inline Control: [tag] syntax in the text, free-form natural language, word-level. Verified: a 10-marker demo transcribed with zero markers spoken, and per-segment measurement shows them acting ([whisper] -24.9 dBFS/144 Hz vs [shouting] -21.8/165 Hz, [sigh] quietest at -25.6). Modulation not transformation (~3 dB, ~2.4 st). So an LLM can emit arbitrary tags like [conspiratorial, leaning in].
- 46 | 2026-08-01 13:19:08: Marker limits pinned down. Listener: [laughing], [sigh], [emphasis] work; [shouting], [excited], [whisper] and free-form descriptors do not. Cause: loudness range is ~9-11 dB in EVERY config tested — one marker per batch, contrasting markers in one batch, intensified wording, and reference-free. A property of the decoder, not something markers move. Reference-free was worst (F0 81-100 Hz), so the reference is not suppressing them.
- 47 | 2026-08-01 13:19:08: Guidance for LLM-generated fish text: emit discrete-event and prominence tags ([laughing], [emphasis], [short pause]); skip style descriptors, which cost chunk_length bytes for nothing. Volume dynamics belong in post gain; delivery comes from the reference clip. Open thread: contrast-cloned hit F0 210 vs ~188 elsewhere — pitch excursion may respond to within-batch contrast even though loudness does not. One data point.
- 48 | 2026-08-01 14:19:25: Marker register recorded in scripts/tts-gen/README.md. Works: [laughing], [confident], [curious]; works-but-misnamed: [whisper] (=soft aside, not whisper), [shouting] (=mildly emphatic, not volume). Partial: [soft tone], [determined], [very excited], [emphasis]. No effect: [whispering] (the OFFICIAL spelling, while undocumented [whisper] works), [excited], [sarcastic], [break]/[long-break], [sad][whispering] stacking. [slow] works only on artificial staccato text, not real prose.
- 49 | 2026-08-01 14:19:30: exp_official_tags.py (all 108 markers on one shared neutral carrier + spoken index) came back INCONCLUSIVE — listener heard 1-35 as identical. Probable causes: the carrier is emotionally neutral so nothing for [grateful] to attach to, and 20+ near-identical turns accumulate so the model locks into one delivery. Hypothesis: tags bite only when sentence CONTENT is congruent. Next: short congruent sentence per tag, few per render.
