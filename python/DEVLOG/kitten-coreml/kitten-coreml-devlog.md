- 1 | 2026-04-18 02:02:54: v27 experiment harness: added KITTEN_TRIM_MODE env dispatcher in KittenCoreMLEngine with five strategies (v26/bounded-back/fwd-last-loud/fwd-extend/burst-scan/aggressive) plus helpers frameRms, frameZcr, framePeakRms. Goal: generate a listening set that covers the overtrim/undertrim tradeoff space so we can pick a direction instead of tuning blind.
- 2 | 2026-04-18 02:02:59: candidate (a) v26 baseline — the current live detector. Reference point. Duration 60.84s, 18 clicks by the Python detector (click count drops here because v26's overtrim cuts audio off before the detector would register the discontinuity).
- 3 | 2026-04-18 02:03:05: candidate (b) bounded-back-500 — v26 backward scan but the lookback is capped at 500 ms so it can't walk past the current sentence's tail into an earlier loud word. When the bounded scan finds nothing, it falls back to rawLen − 80 ms instead of rawLen. Should fix the 'whispered' undertrim without trading it for worse overtrim on 'folded note'/'distance'.
- 4 | 2026-04-18 02:03:11: candidate (c) fwd-last-loud-100 — scans FORWARD over the last 500 ms for the latest frame whose 10 ms RMS exceeds peak × 0.3 (−10 dB). Trims at last-loud + 100 ms. The generous 100 ms margin is meant to swallow short unvoiced word-finals (/t/, /d/, /s/) that a stricter speech detector would mistake for silence or burst.
- 5 | 2026-04-18 02:03:17: candidate (d) fwd-extend — first pass finds last-loud like (c). Second pass walks forward through quiet-but-present frames (RMS >= peak × 0.02, ~−34 dB). STOPS extending at any frame whose RMS sits in the quiet band AND zero-crossing rate is >= 0.45 — that's the vocoder burst signature. Intended for whispered-style sentences where the entire /spərd/ tail is quiet but valid speech.
- 6 | 2026-04-18 02:03:22: candidate (e) burst-scan — classifier over the last 300 ms. Every 10 ms frame labelled speech (rms >= peak × 0.3), burst (rms >= peak × 0.002 AND zcr > 0.45), or silence. Cuts at the leading edge of the earliest detected burst run. Targets the burst directly instead of inferring it from where speech ends. Fallback rawLen − 80 ms if no burst found.
- 7 | 2026-04-18 02:03:28: candidate (f) aggressive-80 — no detector at all. Just trims 80 ms off every sentence-final chunk. Baseline question: does a flat chop of ~80 ms reliably kill the burst without unacceptable clipping? If yes, that's a strong indicator that clever detection is overkill and we should just accept a fixed 80 ms tail haircut.
- 8 | 2026-04-18 02:03:32: candidate (g) aggressive-150 — flat 150 ms chop. Almost certainly will clip final stops (expect 'read', 'note', 'distance' to sound truncated). Included as the upper bound on 'how much can we cut before it sounds wrong' so we can triangulate the sweet spot between (f) and (g).
- 9 | 2026-04-18 02:03:54: candidate (h) longfade-80 — don't try to detect the burst; cover it. Fixed 20 ms trim plus a 80 ms linear fadeOutTail on the last 80 ms of every sentence-final chunk. The fade ramp attenuates anything in the last 80 ms to 0 gradually, so a burst sitting in that window becomes inaudible without needing to know exactly where it is. Cost: the natural decay of the final word also gets faded.
- 10 | 2026-04-18 02:04:00: candidate (i) longfade-150 — same idea as (h) but the fade window is 150 ms. Covers bursts that sit further back from rawLen (if the model's post-roll extends that far) at the cost of a more audible ramp-out. Gives us a second data point along the fade-length axis to sweep.
- 11 | 2026-04-18 02:04:08: candidate set available at outputs/candidates/{a-i}.wav with README. Durations: (a)=60.8s (overtrim steals content); (b–i)=65.2–66.4s (roughly equal, varying where the tail cut lands). Python detector click counts: (a)=18 but that's misleadingly low because v26's overtrim removes audio the detector would have flagged. (b)=44 (g)=36 (f)=42. Listen-test will be the decisive metric; aggregate click counts across whole files are too coarse to separate these.
